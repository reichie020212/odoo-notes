## Consignment

store and sell products for vendors without having to buy them

charge a fee for storing products your company doesn't own

enable `consignment` in config settings

create `receipt` order by going to overview -> receipt card

select the vendor in `receive from` field

select the same vendor in the `assign owner` field

add product and quantity then validate

consignment product affects stock valuation differently then than product we do own.
