## Resupply Warehouse

enable `storage locations` and `multi-step routes` in config settings

go to warehouse form in configuration then find the `resupply from` field and select other warehouse

go to products form -> inventory tab and select the routes of the product

when creating a reordering rules, set the location and route

a delivery order from one warehouse to another will be created based on the reordering rule
