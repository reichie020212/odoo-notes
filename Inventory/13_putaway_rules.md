## Putaway Rules
### redirects product location

Enable `storage locations` and `multi-step routes`

Go to configuration -> Putaway Rules

Add Putaway Rules

This will redirect a product or product category to other location

on the PO stock picking, go to `moves` smart button to check if the putaway rules take place
