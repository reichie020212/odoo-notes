enable `barcode scanner` and `delivery methods` in inventory settings

enable also `storage locations` and `multi-step routes`

to change incoming and outgoing shipments, go to inventory -> configuration -> warehouse

add barcode to products form

in the barcode app, scan the pickings barcode to automatically go to the pickings

explore barcode app, there are lots of things there

you can add barcode to the warehouse location

scan product barcode to increase product count

click validate once done
