Enable `Purchase Agreement` and `Purchase Alternative` in Purchase settings

GO to RFQ Form -> Alternatives tab -> Create alternatives

Click `compare product lines` to check differences

When confirming RFQ, a popup will show that asks if alternative rfq should be cancelled or keep
